/**
 * The fallback banner used when no cover photo is set. Same treatment as the
 * coach public page (app/c/[handle]/page.tsx) so a community banner and a
 * coach banner look like the same product.
 */
export const DEFAULT_COVER_GRADIENT =
  "bg-[linear-gradient(120deg,oklch(0.72_0.16_65)/25,oklch(0.65_0.18_25)/25,oklch(0.7_0.14_200)/25)]";
